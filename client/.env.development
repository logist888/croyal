# Local dev: the Vite dev server (5173) talks to the standalone game server (3001).
# Production builds leave this empty -> same-origin (server serves the built client).
VITE_API_BASE=http://localhost:3001
